# 用户指令序列

按时间整理。**来源：`../memory/` 工作日志**，保留需求要点，非逐字转录。

---

## 2026-09-17 — 项目起点：先把课表数据搞到手

> **需求**：写一个爬虫，登录石河子大学教务系统抓课表，导出 JSON。
> 教务系统有统一身份认证（CAS）+ 短信二次认证。

**产出**：`../../tools/jwgl_spider.py`
- CAS 密码加密（复现前端 `encrypt.js` 的 AES-CBC）
- 短信二次认证（MFA）
- 解析 `xskb_list.do` 的周课表网格
- 导出 `课表.json`（样例见 `../../docs/课表.json`）

**踩坑**：`jwgl.shzu.edu.cn` 服务器 TLS 较旧，Python `requests`（OpenSSL 3.5+）握手会因
后量子密钥交换 `X25519MLKEM768` 失败 → 改用 `curl_cffi`（`impersonate="chrome"`）模拟 Chrome TLS 指纹。

---

## 2026-09-18 — 从「抓数据」转向「做 App」，然后彻底换技术栈

### ① Flutter 版（已废弃）

> **需求**：基于爬到的课表做一个 App。

**产出**：`石大课表_Flutter原型.apk`（Flutter + Dio + Provider）

### ② 401 修复

> **用户反馈**：登录时报 `DioException [bad response]: 401`。

**根因**：Dio 开 `followRedirects: true` 时，CAS 重定向链中间 302 的 `Set-Cookie` 不经过拦截器，
jwgl 会话丢失。→ 关掉自动重定向，手动跟随（最多 12 跳）。
详见 `../../Backups/版本说明_Flutter原型.md`。

### ③ 重大重构：改用 Kotlin + Compose + MiuiX ⭐

> **用户要求**：
> - 去掉 App 内登录 UI；
> - 改为**内嵌 WebView 打开教务系统**，用户在网页里登录后点「导入」抓取当前页面课表；
> - UI 换成 **MiuiX** 风格（https://github.com/compose-miuix-ui/miuix）；
> - 去掉页面标语。

**产出**：`shzu_class_km/`（此后一直是唯一正式工程）+ `石大课表_MiuiX首版.apk`
体积 57 MB → 14.6 MB。

### ④ WebView 修复

> **用户反馈**：WebView 进入教务系统显示不正常，选不到「学期理论课表」。

**产出**：`石大课表_MiuiX首版_WebView修复.apk`（只改 `ui/ImportPage.kt`）

### ⑤ v1.1 大版本

> **需求**：包名 / 应用名变更 + 功能大扩充。

**产出**：`石大超级课表_BETA-v1.1.apk`（包名定为 `com.shzu.superschedule`，应用名定为「石大超级课表」）
今日页 / 周课表页成型、配色、周次计算、学期切换、四种桌面小组件。

### ⑥ v1.2

> **需求**：20 项 bug 修复 / 功能。

**产出**：`石大超级课表_BETA-v1.2.apk`

### ⑦ v1.3

> **需求**：15 项功能。

**产出**：`石大超级课表_BETA-v1.3.apk`（10×7 网格模型、二级页返回、底栏首次加模糊）

---

## 2026-09-19 — v1.3.1 与 v1.3.2

### ① v1.3.1

> **需求**：课表布局彻底做严（行距相等、长格不被压）、修小组件、修闪退。

**产出**：`石大超级课表_BETA-v1.3.1.apk`

### ② v1.3.2 主体迭代

> **需求（多条，按提出顺序）**：
> - 教师显示：从「1 位 / 全部」改为「可指定任意某位」；
> - 预测性返回：「划到位但还没松手时动画应当已播完」
>   （用户反馈松手才动 / 补播一段很割裂）；
> - **底栏要真实的背景高斯模糊（磨砂玻璃）**；
> - 「依然没有模糊效果」（反复反馈，推动了两次根因定位）；
> - **「模糊过于生硬，请叠上一层渐变底色」**；
> - 通知测试相关：随机取一节课、长按可关闭、连点第 3 次起提示剩余次数；
> - **「toast 文案显示时间缩短至 0.5s」**；
> - **「toast 延长至 1.2s」**（0.5s 实测偏短后的修正）；
> - 「改好 toast 时长后我自己测试，停止任务」。

**产出**：`石大超级课表_BETA-v1.3.2.apk`（构建序列 v140 → v145）

### ③ 工程整理（本次）

> **需求**：
> - 整理工程文件，删掉已弃用内容，剩余文件整理为项目需要的内容；
> - release APK 新建 `Backups` 备份，并在 `Backups` 下写好每个版本详细改动声明；
> - 把所有思考链、改动过程、提示词、日志等历史记录保存到工程目录下**一个新文件夹**，
>   并在项目 README 中声明其作用和位置；
> - 为项目写一个 **Prompt 声明**，保证文件夹放到其他 Agent 也可顺利运行。

**产出**：`../../Backups/`、`../../History/`、`../../README.md`、`../../PROJECT_PROMPT.md`

---

## 2026-09-21 — 教务查询打通、构建脚本合并

### ① 教务查询三项接入（分支 `教务查询接入`）

> **需求**：把「考试安排」「课程成绩」「等级考试成绩」三项教务查询结果接进 App。
>
> **过程中的关键反馈**：
> - 「v156 装了没装、真机效果都没验」→ 反复要求 `adb 安装好我测试`；
> - 「提交 git 与 github」；
> - 「合入 main 吧，v159 基本正常，复验通过，修改好诊断开关，
>    tools/ 下 ps1 文件合并成一个带版本参数的脚本，去除重复内容，改动同步提交到 GitHub」。

**产出**：
- `data/JwglQueryFetcher.kt` / `data/QueryHtmlFetcher.kt` / `ui/WebViewFetcher.kt`
  —— 后台抓取改走 **WebView 内 JS fetch**（`HttpURLConnection` 拿不到 `JSESSIONID`）；
- `data/QueryStore.kt` —— 查询结果落盘持久化；
- `ui/QueryPage.kt` —— 查询页一级/二级页；
- 诊断开关 `DUMP_TARGET_FRAME` 关掉；
- **27 个逐字节相同的 `tools/build_km_v*.ps1` 合并为带 `-Version` 参数的单一入口
  `tools/build_km.ps1`**。

---

## 2026-09-22 — 查询页排版批改 → 发版 BETA-v1.4

### ① 七条排版修改（一次性提出）

> **需求（原话要点）**：
> 1. 删除 `tools/build_apk.ps1`；
> 2. 考试安排：时间、考场**加粗、放到上面醒目显示**；
>    考试时间的日期后写好匹配第几周周几，如
>    `2026年11月28日（第13周周六） 10:00-11:30`；
> 3. 课程成绩：精简「开课学期」「课程编号」，**着重显示课程名称**，
>    放到「1」「2」这种小标题的位置加粗，**成绩放大加粗**，**不及格标红**；
> 4. 等级考试：删掉第一条以「笔试」为小标题的无意义内容；考级课程作每条小标题加粗；
>    「分数类成绩」只显示一条有数据的；按 **CET-4 → CET-6 → NCRE一级 → NCRE二级 → 其他** 排序；
> 5. 查询页标题 UI 与设置页**完全一致**（字号、留白）；
> 6. 检查「今日课程」「本周课表」两个大标题是否完全对齐；
> 7. 左右翻动周视图时，**上方日期/星期条也应当跟随滚动**；蓝色「本周」放到「第x周」前；
> 8. 删除「查询」标题下的解释文字。

**产出**：新文件 `ui/QueryCards.kt`（三类结果分类型排版）+ `WeekPage.kt` 重构
（日期条/星期表头移入 `HorizontalPager`）。

### ② adb 推送到设备

> **需求**：`adb 推送到我的设备上`。

**产出**：v162、v163 装机并**用 md5 核对**（设备 `base.apk` 的 md5 与本地产物一致）。

### ③ 等级考试「分数显示 0」

> **用户反馈**：`有问题，等级考试成绩的显示分数需要选择有具体分数的，分数一般不为0`。

**根因**：教务对「这项没有成绩」不是留空而是填 **`0`**；且等级考试表的
第二层表头被写在 `<tbody>` 里，导致 4 列「分数类成绩」同名无法分辨。

**产出**：`JwglQueryParser.repairStrayHeader`（把那一行**当第二层表头**用、补回子列名）
+ `QueryCards.pickGroup` / `isRealValue`（跳过 `0` / `-` / `无`，优先「总成绩」）。

### ④ 发版 BETA-v1.4

> **需求**：`测试无误，版本号修改为BETA-v1.4，改动提交Github，并创建新的release，上传这个版本`。

**产出**：commit `57ce101`、tag `BETA-v1.4`、GitHub Release（prerelease）
+ 附件 `SHZUSuperSchedule_BETA-v1.4.apk`。

### ⑤ 安装最新版到手机

> **需求**：`adb install最新版到手机`。

---

## 2026-09-25 — 再次工程整理 + 保存全部历史 + 重写 Prompt 声明

> **需求**：
> - 再将目前工程文件整理一下，删掉已经弃用的内容，将剩余文件整理为项目需要的内容；
> - 将工程所有的思考链、改动过程、提示词、日志等一切历史记录进行保存；
> - 为项目写一个 **Prompt 声明**，保证文件夹放到其他 Agent 也可顺利运行。

**产出**：
- 根目录 23 份 `km_build_v*.txt` 归入 `History/logs/build/`；
- 调试 dump（`_frame_*.html` / `_wv.txt` / `_u.xml` / `_*.png`）归位到
  `History/logs/device/`、`History/dumps/`、`History/screenshots/`；
- **删除 12 个冗余临时 APK（释放 172 MB）** —— `Backups/` 已存正式归档；
- 弃用的教务爬虫脚本与说明移入 `History/legacy/教务爬虫/`；
- `PROJECT_PROMPT.md` 全面重写（更新到 BETA-v1.4、修正多处已过时的描述）；
- `History/memory/` 与 `.workbuddy/memory/` 同步。

---

## 从指令里能看出的用户风格

1. **反馈非常具体、基于真机体验**：不说「优化一下动画」，而说
   「划到位但还没松手时动画应当已播完」。
2. **会连续追同一个问题直到解决**：模糊效果反馈了不止一次，直到像素级验证通过。
3. **会自己验收并推翻参数**：0.5s → 1.2s 就是这样。
4. **不喜欢被过度自动化**：明确说过「我自己测试，停止任务」——做完就交付，别自作主张继续。
5. **会一次性提出一批需求**（例如 7 条排版修改一起提），期望一次性全部落地，
   然后自己逐条在真机上验收；有问题只报现象 + 判据，不报猜测。
6. **反馈自带判断规则**：不说「分数不对」，而说
   「需要选择有具体分数的，**分数一般不为 0**」—— 判据直接可用，照字面实现即可。
7. **重视"换人也能接手"**：两次提出「整理工程 + 保存历史 + 写一份 Prompt 声明」，
   说明他把这个工程当作要长期交给别人（或别的 Agent）维护的资产，不只是自己用。
