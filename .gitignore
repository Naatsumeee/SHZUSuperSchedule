# ---------- Gradle / Android 构建产物 ----------
.gradle/
build/
shzu_class_km/.gradle/
shzu_class_km/build/
shzu_class_km/app/build/
shzu_class_km/.kotlin/
captures/
.externalNativeBuild/
.cxx/
*.dex

# ---------- 安装包与签名（二进制，默认不入库） ----------
*.apk
*.aab
*.ap_
*.jks
*.keystore

# 例外：Backups/ 下的 release APK 是版本记录的一部分。
# ⚠️ 2026-09-25 起仓库内**不再保留 APK 二进制**（只留最近 2 个的策略已改为全部移出，
# 详见 Backups/README.md）：APK 是可重建产物，长期资产是版本说明 md 与 git 历史，
# 二进制以 GitHub Release 为归档处，每个包的 sha256 记在 Backups/README.md 里。
# 原先这里单独排除 57MB 的 Flutter 原型（超 GitHub 的 50MB 单文件上限），
# 它已被移出仓库，那条特例规则随之作废。
!Backups/*.apk

# ---------- 本地配置 ----------
local.properties

# ---------- 第三方库源码（本地参考用，不入库） ----------
# MiuiX 官方仓库：https://github.com/compose-miuix-ui/miuix
# 需要查 API 时直接从上游获取，不要把它复制进仓库。
reference/

# ---------- IDE ----------
*.iml
.idea/
.vscode/

# ---------- 系统 ----------
.DS_Store
Thumbs.db

# ---------- 日志与临时 ----------
*.log

# 构建日志：散在根目录的不入库（随时可重新生成），
# 归档进 History/logs/build/ 的**要入库**（那是历史记录的一部分）。
#
# ⚠️ 上面第 3 行的 `build/` 会连带忽略**任何层级**下叫 build 的目录，
#    所以这里必须先把 `History/logs/build/` 这个目录本身解禁，
#    再解禁里面的日志文件名 —— 只写后者是不生效的
#    （git 规则：父目录被排除时，无法靠否定规则重新纳入其中的文件）。
!History/logs/build/
km_build_v*.txt
!History/logs/build/km_build_v*.txt
km_test.txt

# ---------- Python（教务爬虫） ----------
__pycache__/
*.pyc
.venv/
venv/

# 手动测试用的临时导出包（不入库）
*教务查询测试版*.apk

# ---------- 根目录下划线前缀的临时调试残留 ----------
# 排查教务页面结构时随手 dump 的截图 / uiautomator xml / logcat / iframe HTML。
# 这些文件里可能夹带教务页面里的个人化标识，且随时可重新生成，一律不入库。
/_*.png
/_*.xml
/_*.txt
/_*.html
